apiVersion: v1
kind: Service
metadata:
  name: incident-copilot-app
  namespace: incident-copilot
spec:
  selector: { app: incident-copilot-app }
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
